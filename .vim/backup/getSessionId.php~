<?php
    include('/slim/inc/key.php');
    include('/slim/opentok-php-sdk/OpenTokSDK.php');
    include('/slim/opentok-php-sdk/API_Config.php');

//    echo(console);

    $apiObj = new OpenTokSdk($api_key, $secret);
    // $session = $apiObj->createSession($_SERVER["REMOVE_ADDR"]);
    $session = $apiObj->createSession( $_SERVER["REMOTE_ADDR"], array(SessionPropertyConstants::P2P_PREFERENCE=> "enabled") );  
    $sessionId = $session->getSessionId();
    echo($sessionId);
?>

/*
   Indraneel Purohit
   --
   mystery.c implements the Fibonacci sequence
*/

#include<stdio.h>

int mystery(int n){
    int a = 0;
    int b = 1;
    int sum;
    int i;
    if (n == 0) {
	return 0;
    }
    for (i=0; i<n; i++){
	sum = b;
	b = a + b;
	a = sum;
    }
    return sum;
}
